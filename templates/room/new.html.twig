{% extends 'base.html.twig' %}


{% block title %}New Room
{% endblock %}

{% block body %}
	<section class="bg-white dark:bg-gray-900 h-screen mx-auto justify-center">
		<div class="container flex items-center justify-center min-h-screen px-6 mx-auto max-w-md">
			<div class="w-full max-w-md">
				<h1 class="py-10 text-3xl font-semibold text-gray-700 text-center dark:text-slate-50">Create a new Room</h1>

				{{ include('room/_form.html.twig') }}
				<div class="mt-3">
					<a href="{{ path('app_room_index') }}" class="font-medium text-gray-700 dark:text-slate-50 hover:underline capitalize ">back to list</a>
				</div>
			</div>
		</div>
	</section>
{% endblock %}
