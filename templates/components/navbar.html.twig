{% block header %}

	<nav class="bg-white shadow dark:bg-gray-800">
		<div class="container flex items-center justify-center p-6 mx-auto text-gray-600 capitalize dark:text-gray-300 space-x-6">
			<a href="{{ path("app_login")}}" class="text-gray-800 transition-colors duration-300 transform dark:text-gray-200 border-b-2 hover:border-blue-500 {% if app.request.get('_route') == 'app_login' %} border-blue-500{% endif %}">Log</a>

			<a href="{{path("app_reservation_index")}}" class="border-b-2 hover:text-gray-800 transition-colors duration-300 transform dark:hover:text-gray-200 hover:border-blue-500 {% if app.request.get('_route') == 'app_reservation_index' %} border-blue-500{% endif %} ">Reservation</a>

			<a href="{{ path('app_logout') }}">
				<button class="rounded-md py-2 px-3 bg-slate-50 text-black font-semibold hover:bg-slate-300">
					Logout
				</button>
			</a>
		</div>
	</nav>

{% endblock %}
